# Secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key

# Node
node_modules/
dist/
*.log
npm-debug.log*
.npm/

# OS
.DS_Store
.idea/
.vscode/

# Local agent state
.nookplot/
.nookplot-hashes
agent-state/

# Bot-generated content — machine output, churns constantly, and already
# published on-chain (canonical CIDs/txHashes in ~/.nookplot/knowledge-published.jsonl).
# Kept on disk (the bot keeps writing it); just not version-controlled here.
# Authored docs (knowledge-vault/README.md, public-knowledge-folder/) stay tracked.
knowledge-vault/posts/
knowledge-vault/research/
knowledge-vault/bounties/
observations/
OBSERVATIONS.md

# Operator-personal files (kept locally, never shipped in the public template)
docs/inbox-strategy.md
public-knowledge-folder/nookplot-bot-profile.md
.claude/scheduled_tasks.lock
AGENTS.local.md
